<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tienda">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyCarrito"></activity>
        <activity android:name=".VentaUnitaria" />
        <activity android:name=".VentaObjetos" />
        <activity android:name=".ResetPasswordActivity" />
        <activity
            android:name=".TiendaHome"
            android:label="@string/title_activity_tienda_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Registrar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>